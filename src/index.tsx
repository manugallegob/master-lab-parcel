import React from "react";
import { createRoot } from "react-dom/client";
import './mystyles.scss';

const root = createRoot(document.getElementById("root")!);
root.render(
<h2>Este fragmento de código ha sido generado con React</h2>
);
